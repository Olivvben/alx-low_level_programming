#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: number being checked
 * Return: always 0
 */

int _abs(int n)
{
		int n;

		if (n < 0)
		{
			return (-n);
		}
		else
		{
			return (n);
		}
}
